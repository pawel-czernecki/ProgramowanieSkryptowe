doctype html
html(lang='en')
    head
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width, initial-scale=1')
        title Second Express application
        style.
            table {
                width: 100%;
            }
            td {
                border: 1px solid #000; 
                padding: 15px;
                text-align: left;
            }
            th {
                background-color: #04AA6D;
                color: white;
            }
    body
        main
            h1 Rezerwacja pokoju
            form(method='POST' action='/client/add-reservation')
                label(for='client') Podaj imie i nazwisko
                input(name='client')
                br
                label(for='room') Wybierz pokój
                if rooms && rooms.length > 0
                    select(name='room')
                        each room in rooms
                            option(value=room.id) #{room.id}
                else
                    p Uwaga! Nie znaleziono żadnych pokoi
                br
                input(type='submit')